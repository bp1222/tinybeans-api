post:
  tags:
    - auth
  summary: Login to Tinybeans
  operationId: login
  requestBody:
    description: Login Information
    content:
      application/json:
        schema:
          $ref: '../schema/authenticate_request.yaml'
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '../schema/authenticate_response.yaml'