get:
  tags:
    - followings
  summary: Tinybeans Followings
  operationId: followings 
  security:
    - ApiKeyAuth: []
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '../schema/followings.yaml'